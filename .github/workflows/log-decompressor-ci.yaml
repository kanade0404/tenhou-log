name: Log Decompressor CI
on:
  pull_request:
    paths:
      - services/log_decompressor/**
      - pkg/**
      - .github/workflows/log-decompressor-ci.yaml
      - go.*
      - '!pkg/parser/**'

jobs:
  services:
    uses: kanade0404/tenhou-log/.github/workflows/function-ci.yaml@master
    with:
      service_name: log_decompressor

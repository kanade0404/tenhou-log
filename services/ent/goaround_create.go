// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/kanade0404/tenhou-log/services/ent/goaround"
)

// GoAroundCreate is the builder for creating a GoAround entity.
type GoAroundCreate struct {
	config
	mutation *GoAroundMutation
	hooks    []Hook
}

// SetID sets the "id" field.
func (gac *GoAroundCreate) SetID(u uuid.UUID) *GoAroundCreate {
	gac.mutation.SetID(u)
	return gac
}

// SetNillableID sets the "id" field if the given value is not nil.
func (gac *GoAroundCreate) SetNillableID(u *uuid.UUID) *GoAroundCreate {
	if u != nil {
		gac.SetID(*u)
	}
	return gac
}

// Mutation returns the GoAroundMutation object of the builder.
func (gac *GoAroundCreate) Mutation() *GoAroundMutation {
	return gac.mutation
}

// Save creates the GoAround in the database.
func (gac *GoAroundCreate) Save(ctx context.Context) (*GoAround, error) {
	var (
		err  error
		node *GoAround
	)
	gac.defaults()
	if len(gac.hooks) == 0 {
		if err = gac.check(); err != nil {
			return nil, err
		}
		node, err = gac.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*GoAroundMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = gac.check(); err != nil {
				return nil, err
			}
			gac.mutation = mutation
			if node, err = gac.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(gac.hooks) - 1; i >= 0; i-- {
			if gac.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = gac.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, gac.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*GoAround)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from GoAroundMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (gac *GoAroundCreate) SaveX(ctx context.Context) *GoAround {
	v, err := gac.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (gac *GoAroundCreate) Exec(ctx context.Context) error {
	_, err := gac.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (gac *GoAroundCreate) ExecX(ctx context.Context) {
	if err := gac.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (gac *GoAroundCreate) defaults() {
	if _, ok := gac.mutation.ID(); !ok {
		v := goaround.DefaultID()
		gac.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (gac *GoAroundCreate) check() error {
	return nil
}

func (gac *GoAroundCreate) sqlSave(ctx context.Context) (*GoAround, error) {
	_node, _spec := gac.createSpec()
	if err := sqlgraph.CreateNode(ctx, gac.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	return _node, nil
}

func (gac *GoAroundCreate) createSpec() (*GoAround, *sqlgraph.CreateSpec) {
	var (
		_node = &GoAround{config: gac.config}
		_spec = &sqlgraph.CreateSpec{
			Table: goaround.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: goaround.FieldID,
			},
		}
	)
	if id, ok := gac.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	return _node, _spec
}

// GoAroundCreateBulk is the builder for creating many GoAround entities in bulk.
type GoAroundCreateBulk struct {
	config
	builders []*GoAroundCreate
}

// Save creates the GoAround entities in the database.
func (gacb *GoAroundCreateBulk) Save(ctx context.Context) ([]*GoAround, error) {
	specs := make([]*sqlgraph.CreateSpec, len(gacb.builders))
	nodes := make([]*GoAround, len(gacb.builders))
	mutators := make([]Mutator, len(gacb.builders))
	for i := range gacb.builders {
		func(i int, root context.Context) {
			builder := gacb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*GoAroundMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, gacb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, gacb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, gacb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (gacb *GoAroundCreateBulk) SaveX(ctx context.Context) []*GoAround {
	v, err := gacb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (gacb *GoAroundCreateBulk) Exec(ctx context.Context) error {
	_, err := gacb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (gacb *GoAroundCreateBulk) ExecX(ctx context.Context) {
	if err := gacb.Exec(ctx); err != nil {
		panic(err)
	}
}

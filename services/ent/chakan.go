// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/google/uuid"
	"github.com/kanade0404/tenhou-log/services/ent/call"
	"github.com/kanade0404/tenhou-log/services/ent/chakan"
)

// Chakan is the model entity for the Chakan schema.
type Chakan struct {
	config
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ChakanQuery when eager-loading is set.
	Edges ChakanEdges `json:"edges"`
}

// ChakanEdges holds the relations/edges for other nodes in the graph.
type ChakanEdges struct {
	// Call holds the value of the call edge.
	Call *Call `json:"call,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CallOrErr returns the Call value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChakanEdges) CallOrErr() (*Call, error) {
	if e.loadedTypes[0] {
		if e.Call == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: call.Label}
		}
		return e.Call, nil
	}
	return nil, &NotLoadedError{edge: "call"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Chakan) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case chakan.FieldID:
			values[i] = new(uuid.UUID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Chakan", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Chakan fields.
func (c *Chakan) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case chakan.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				c.ID = *value
			}
		}
	}
	return nil
}

// QueryCall queries the "call" edge of the Chakan entity.
func (c *Chakan) QueryCall() *CallQuery {
	return (&ChakanClient{config: c.config}).QueryCall(c)
}

// Update returns a builder for updating this Chakan.
// Note that you need to call Chakan.Unwrap() before calling this method if this Chakan
// was returned from a transaction, and the transaction was committed or rolled back.
func (c *Chakan) Update() *ChakanUpdateOne {
	return (&ChakanClient{config: c.config}).UpdateOne(c)
}

// Unwrap unwraps the Chakan entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (c *Chakan) Unwrap() *Chakan {
	_tx, ok := c.config.driver.(*txDriver)
	if !ok {
		panic("ent: Chakan is not a transactional entity")
	}
	c.config.driver = _tx.drv
	return c
}

// String implements the fmt.Stringer.
func (c *Chakan) String() string {
	var builder strings.Builder
	builder.WriteString("Chakan(")
	builder.WriteString(fmt.Sprintf("id=%v", c.ID))
	builder.WriteByte(')')
	return builder.String()
}

// Chakans is a parsable slice of Chakan.
type Chakans []*Chakan

func (c Chakans) config(cfg config) {
	for _i := range c {
		c[_i].config = cfg
	}
}

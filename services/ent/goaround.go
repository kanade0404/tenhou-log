// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/google/uuid"
	"github.com/kanade0404/tenhou-log/services/ent/goaround"
)

// GoAround is the model entity for the GoAround schema.
type GoAround struct {
	config
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GoAround) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case goaround.FieldID:
			values[i] = new(uuid.UUID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type GoAround", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GoAround fields.
func (ga *GoAround) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case goaround.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ga.ID = *value
			}
		}
	}
	return nil
}

// Update returns a builder for updating this GoAround.
// Note that you need to call GoAround.Unwrap() before calling this method if this GoAround
// was returned from a transaction, and the transaction was committed or rolled back.
func (ga *GoAround) Update() *GoAroundUpdateOne {
	return (&GoAroundClient{config: ga.config}).UpdateOne(ga)
}

// Unwrap unwraps the GoAround entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ga *GoAround) Unwrap() *GoAround {
	_tx, ok := ga.config.driver.(*txDriver)
	if !ok {
		panic("ent: GoAround is not a transactional entity")
	}
	ga.config.driver = _tx.drv
	return ga
}

// String implements the fmt.Stringer.
func (ga *GoAround) String() string {
	var builder strings.Builder
	builder.WriteString("GoAround(")
	builder.WriteString(fmt.Sprintf("id=%v", ga.ID))
	builder.WriteByte(')')
	return builder.String()
}

// GoArounds is a parsable slice of GoAround.
type GoArounds []*GoAround

func (ga GoArounds) config(cfg config) {
	for _i := range ga {
		ga[_i].config = cfg
	}
}
